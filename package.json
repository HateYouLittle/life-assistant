{
  "name": "life-assistant",
  "version": "0.1.0",
  "description": "Personal life assistant (Skill + MCP server) for Hermes Agent: weather alerts, oil price notices, express tracking.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:scheduler": "node dist/scheduler.js",
    "dev": "tsx src/index.ts",
    "dev:scheduler": "tsx src/scheduler.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "node-cron": "^3.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
